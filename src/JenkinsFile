pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                echo 'hello there'
                sh 'npm install'
                sh 'npm build'
            }
        }
    }
}